# Update Node
sudo npm cache clean -f
sudo npm install -g n
sudo n stable

# Update NPM
sudo npm install -g npm

# Install favorite Node packages
sudo npm install -g bower express-generator grunt-cli gulp jshint yo

# Update all modules
sudo npm update -g
